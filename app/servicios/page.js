import {CheckCircle2} from 'lucide-react';import ServiceIcon from '../../components/ServiceIcon';import Link from 'next/link';import PageHero from '../../components/PageHero';import Reveal from '../../components/Reveal';import {services} from '../../lib/content';
export const metadata={title:'Servicios'};
export default function Servicios(){return <><PageHero variant="services" eyebrow="Capacidades" title="Soluciones conectadas, no servicios aislados." description="Combinamos las especialidades necesarias para diseñar, implementar y mantener el ecosistema tecnológico de tu organización."/><section className="section light"><div className="container service-detail-list">{services.map((s,i)=><Reveal className="service-detail" id={s.slug} key={s.slug}><div className="service-number">0{i+1}</div><div className="service-detail-copy"><ServiceIcon slug={s.slug}/><h2>{s.title}</h2><p>{s.summary}</p></div><ul>{s.details.map(x=><li key={x}><CheckCircle2 size={14}/>{x}</li>)}</ul><Link href={`/cotizacion?servicio=${s.slug}`}>Solicitar información →</Link></Reveal>)}</div></section></>}
