# URL base del backend (NestJS). Debe incluir el prefijo /api.
# ¡Se hornea en tiempo de build! (variable NEXT_PUBLIC_*)
# Producción:
NEXT_PUBLIC_API_URL=https://api.pascare.tech/api
# Desarrollo local (backend en el puerto 4000):
# NEXT_PUBLIC_API_URL=http://localhost:4000/api
